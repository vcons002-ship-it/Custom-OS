config BR2_PACKAGE_CLADE
	bool "clade"
	help
	  Clade's userland: weaved (PID 1), the mind-plane services, and the
	  Weave. Prebuilt static (musl) binaries from the workspace at the
	  repository root; see package/clade/clade.mk.
