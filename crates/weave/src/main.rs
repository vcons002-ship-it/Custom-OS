//! weave — the native userland (docs/07-interaction-model.md).
//!
//! M0: not yet supervised; the banner belongs to weaved. M1 draws the first
//! DRM/KMS frame; M2 prototypes wgpu vs. an embedded web engine against the
//! mockup animation set and locks the renderer choice.

fn main() {
    println!("[clade:weave] renderer decision lands at M2 (wgpu vs. embedded web engine)");
}
